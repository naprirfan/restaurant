import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const ResultDetailScreen = ({ result }) => { 

  return (
    <View>
      <Text>ResultDetailScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
});

export default ResultDetailScreen;
